import 'server-only'

import * as clears from './clears'
import * as squads from './squads'
import * as users from './users'

const RaidStudioAPI = {
  users,
  squads,
  clears,
}

export default RaidStudioAPI
