import * as characters from './characters'
import * as profiles from './profiles'
import * as raids from './raids'
import * as squads from './squads'

export const RaidStudioAPI = {
  characters,
  profiles,
  squads,
  raids,
}
